All the classes are ready for the units tests.


The application uses MySQL database created on the local machine.


Also the src folder contains the java classes. 

Other folders are to support the gui properties.